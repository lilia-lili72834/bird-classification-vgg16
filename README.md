# Bird Species Classification using VGG16

This project implements a convolutional neural network for bird species classification using transfer learning with the VGG16 architecture.

Framework: TensorFlow / Keras  
Dataset: Kaggle Bird Species Dataset  

The model classifies bird images into six different species.
